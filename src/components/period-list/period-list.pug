include ../period/period

mixin period-list(options)
  -
    options = options || {};
    options.periods = options.periods || [];

  ul.period-list
    each periodItem in options.periods
      li.period-list__list-item
        .period-list__mark
        .period-list__period-wrapper
          +period(periodItem)
            | !{periodItem.content}
